<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.grampanchyat"
    android:theme="@android:style/Theme.NoTitleBar.Fullscreen">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashscreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".UserHomeActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".GalleryActivity" />
        <activity android:name=".RegisterNewActivity" />
        <activity android:name=".AboutVillageActivity" />
        <activity android:name=".AboutGrampActivity" />
        <activity
            android:name=".User_home_activity"
            android:label="@string/title_activity_user_home_activity"
            android:theme="@style/AppTheme" />
        <activity android:name=".certificate_activity" />
        <activity android:name=".feedback" />
        <activity android:name=".generalnews" />
        <activity android:name=".MainNotificationActivity"></activity>


        <service android:name=".MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
        <service android:name=".MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT"/>
            </intent-filter>
        </service>


    </application>

</manifest>